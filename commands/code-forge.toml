description = "在一个可验证的目标上跑对抗回环（提方案 ⇄ 找反例 ⇄ 代码判定），过程直播到本地监控台"
prompt = """
用 code-forge 技能在这个目标上跑一次对抗回环：{{args}}

按技能里的协议走：先确认判据命令（一条现在就能跑、能反映目标的命令；有量的话配上 metric），
再 loop_begin 开局并把监控台网址告诉我，然后每轮让提议者与反驳者各自做事、各自 loop_say，
一轮结束调 loop_gate。

记住三条：达标只有 loop_gate 能判；判据不许为了达标而放宽或换掉；停了要如实说是七种原因里的哪一条。
"""
